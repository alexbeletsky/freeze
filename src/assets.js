var assets = {
    initialize: function (request) {
        this.request = request;
    },

    extract: function (url, callback) {
        callback (null, {});
    }

};

module.exports = assets;